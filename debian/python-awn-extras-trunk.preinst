#!/bin/sh
# TODO: remove this file after releasing Squeeze
set -e
if [ "$1" = upgrade ]
then
	if dpkg --compare-versions "$2" lt 0.3.9-1; then
		pycentral pkgremove python-awnlib-trunk python-awn-extras-trunk
	fi
fi

#DEBHELPER#
