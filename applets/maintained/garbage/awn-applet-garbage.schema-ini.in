[DEFAULT/confirm_empty]
type = boolean
default = true
_description = Enable the confirmation dialog form when one wishes to empty the trash.

[DEFAULT/show_count]
type = boolean
default = true
_description = Show the number of items in the trash on top of the icon.

# vim: set ft=cfg :
