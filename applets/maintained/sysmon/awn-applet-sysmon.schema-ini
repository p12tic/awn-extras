[DEFAULT]
single_instance = false

[DEFAULT/dummy]
type = boolean
default = true
description = Just a dummy key.. this will be removed later.

[DEFAULT/time_stamp]
type = integer
default = 0
description = Just a dummy key.. this will be removed later.

[applet/dummy]
type = boolean
default = true
description = Just a dummy key.. this will be removed later.

[applet/icon_list]
type = list-string
default = CPU::-1;
description = List of icons

[applet/time_stamp]
type = integer
default = 0
description = Just a dummy key.. this will be removed later.

[/icons/CPU/graph_type]
type = integer
default = 0
description = CPU graph type to use.  Note that some Icons do not support all graph types.  If this occurs an icon will revert to it's own default (value of 0).

[/icons/CPU/time_stamp]
type = integer
default = 0
description = Just a dummy key.. this will be removed later.

; vim: set ft=dosini :
