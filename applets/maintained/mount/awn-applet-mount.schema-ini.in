[DEFAULT]
single_instance = false

[DEFAULT/hidden-mountpoints]
type = list-string
default = /;swap
_description = List of mountpoints that should remain hidden in the UI.

[DEFAULT/execute-command]
type = string
default = 
_description = Command to execute after mounting a mountpoint.
